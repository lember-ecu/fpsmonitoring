name: lemberfpsmonitor
version: 0.0.3+3
homepage: https://github.com/lember-ecu/fpsmonitoring

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

description: |
  A Flutter package for real-time FPS monitoring in applications. Track and analyze performance, identify issues, and optimize animations. Improve user experience with smooth-running applications. Easy integration for Flutter projects.
