name: lemberfpsmonitor
version: 0.0.3+5
homepage: https://github.com/lember-ecu/fpsmonitoring

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

description: A powerful Flutter package for real-time FPS monitoring and performance analysis in applications.
