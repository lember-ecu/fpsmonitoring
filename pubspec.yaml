name: lemberfpsmonitor
description: A Flutter package for fps monitoring.
version: 0.0.3
homepage: https://github.com/lember-ecu/fpsmonitoring

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
